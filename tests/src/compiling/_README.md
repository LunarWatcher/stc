# Compiling tests

This folder contains tests that only check if things compile. 

In some cases, including the header is sufficient. In other cases, including the header and writing a dummy function for it is required. These are not expected to execute, so it shouldn't be valid.
