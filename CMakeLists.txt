cmake_minimum_required(VERSION 3.10)
project(stc)

# CMake is exclusively used for tests. `add_subdirectory()` with this file will cause stc tests to be built
# ... at least it will when they're added :)
